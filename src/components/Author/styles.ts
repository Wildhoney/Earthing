import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        fontSize: 10,
        fontWeight: 'bold',
        textTransform: 'uppercase',
        marginRight: 20,
        color: 'darkgray',
    },
});
