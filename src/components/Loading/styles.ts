import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        fontSize: 10,
        fontWeight: 'bold',
        textTransform: 'uppercase',
        backgroundColor: '#333',
        color: 'white',
        borderRadius: 5,
        padding: 10,
    },
});
