import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        fontSize: 18,
        fontWeight: 'bold',
        textTransform: 'uppercase',
        marginLeft: 20,
    },
});
